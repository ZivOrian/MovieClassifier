### Issues that need to be addressed
1. I haven't stated anywhere in the epoch function a requires_grad attribute

2. Problem with Early stopping - It stops at it's first iteration, instead of letting the model train more. Try fixing the values. It's probable that the model doesn't fit at all.
